components:
  schemas:
    User:
      type: object
      properties:
        nombreCompleto:
          type: string
          example: Estiven Arboleda
        password:
          type: string
          example: Estiven123!
        fechaNacimiento:
          type: string
          format: date-time
          example: '1991-05-25'
        email:
          type: string
          example: estiven@example.com
        rol:
          type: string
          example: admin || estudiante
        
